<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- شبکه برای OpenAI-compatible API و دانلود مدل‌ها -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- انتخاب فایل محلی (PDF/TXT/GGUF) از طریق Storage Access Framework - نیاز به مجوز ندارد -->
    <!-- خواندن فایل فقط با مجوز زیر برای Android < 13 لازم است؛ در 13+ نیازی نیست -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+: جایگزین محدود READ_MEDIA_* شده؛ فعلاً اختیاری -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_DOCUMENTS" />

    <!-- اشتراک‌گذاری (Share Sheet) برای کارت‌ها و کانفیگ‌ها -->
    <uses-permission android:name="android.permission.NSFilePresentationPreservationAcrossReboots" tools:ignore="ProtectedPermissions"/>

    <application
        android:name=".AuraApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aura"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Activity اصلی که WebView را میزبانی می‌کند -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|locale|rtl|layoutDirection|fontScale|uiMode|density"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Aura">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- فعال‌سازی deep link برای پروتکل‌های vless:// ss:// trojan:// vmess:// -->
        <!-- ذخیره‌سازی کانفیگ به شکل link؛ برای فعال‌سازی اپ‌هایی مثل v2rayNG با لینک مستقیم -->
        <activity-alias
            android:name=".ProxyImportActivity"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vless" />
                <data android:scheme="vmess" />
                <data android:scheme="ss" />
                <data android:scheme="trojan" />
            </intent-filter>
        </activity-alias>

    </application>
</manifest>
